/// camera_set_margin(x,y);

if (argument_count == 0)
    {
    margin_x = 0;
    margin_y = 0;
    }
else if (argument_count == 2)
    {
    margin_x = argument[0];
    margin_y = argument[1];
    }
